# Linux 命令课程

本课程面向进入日本 Web 系统或批处理项目的程序员，默认练习环境为 **Ubuntu 24.04 LTS 或公司提供的 Linux 练习服务器**，使用普通用户和 Bash。

课程目标不是培养服务器管理员，而是让你能够安全地进入既有项目环境，确认文件、权限、进程和日志，并留下可以提交给团队的调查证据。

## 完成后能够做什么

- 确认当前用户、主机、目录和 Linux 发行版
- 在限定目录内创建、复制、移动、搜索和查看文件
- 读懂文件所有者、所属组和 `rwx` 权限，定位常见权限错误
- 查找当前用户启动的进程，读取退出状态并安全停止练习进程
- 使用 `grep`、`less`、`tail` 和 `journalctl` 阅读项目允许访问的日志
- 使用 Vim 完成小范围文件修改，并比较修改前后的差异
- 阅读和维护当前用户的 cron 条目，验证批处理是否执行
- 按日本项目常见格式提交调查命令、结果、影响范围和残课题

## 本课程不覆盖的范围

以下内容通常由基础设施、运维或有权限的负责人处理，本课程只要求能识别相关术语，不要求 PG 独立操作：

- 网络设备、路由、防火墙和 DNS 管理
- 软件包安装、升级和仓库管理
- systemd 服务创建、启停和开机启动设置
- 分区、格式化、挂载和存储扩容
- 用户创建、密码管理和 sudo 策略修改

遇到这些任务时，应先确认作业手顺、权限和负责人，不在共享环境中自行尝试。

## 学习顺序

1. [Linux 环境与命令入门](01_linux_intro.md)
2. [文件、目录与文本命令](02_file_dir_command.md)
3. [用户、组与文件权限](03_user_permission.md)
4. [进程、退出状态与资源确认](04_process_system.md)
5. [用户级定时任务](05_crontab_scheduler.md)
6. [日志查看与调查](06_log_management.md)
7. [Vim 最小编辑操作](07_vi_vim_basic.md)
8. [Web/批处理项目调查演习](08_web_batch_investigation.md)
9. [Linux 命令速查表](linux_command_cheatsheet.md)

## 操作约定

- 普通练习统一在 `~/learndoc-linux-lab` 下完成。
- 命令执行前先运行 `pwd`，删除或修改前再运行 `ls -la -- <目标>`。
- 文中的 `<path>`、`<pid>` 和 `<keyword>` 是占位符，必须替换。
- 不在生产环境执行练习，不修改 `/etc`、`/var` 或其他用户目录。
- 命令失败后先记录命令、错误信息和退出状态，不用 sudo 强行绕过。

```bash
printf 'exit_status=%s\n' "$?"
```

退出状态 `0` 通常表示成功，非 `0` 表示失败或未找到匹配，具体含义以命令说明为准。
